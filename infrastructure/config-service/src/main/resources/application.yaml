spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          # Use the absolute path to your config folder
          search-locations: file:///C:/Projects/IncidentBrain/infrastructure/config-service/src/main/resources/config/

server:
  port: 8888